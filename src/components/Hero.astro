---
import type { Translations } from '../types/translations';

interface Props {
  translations: Translations;
  currentLang: string;
}

const { translations, currentLang } = Astro.props;
const { hero } = translations;
---

<section class="min-h-screen flex items-center justify-center px-6 pt-20 transition-colors duration-300">
  <div class="max-w-4xl mx-auto text-center">
    <div class="mb-8 animate-fade-in" style="animation-delay: 0s;">
      <div class="w-60 h-60 md:w-72 md:h-72 lg:w-80 lg:h-80 rounded-full mx-auto overflow-hidden shadow-2xl border-4 border-white dark:border-gray-700 hover:scale-105 hover:rotate-3 transition-all duration-500 cursor-pointer group">
        <img 
          src="/images/hero/FotoPortfolio.jpg" 
          alt="Joan Bono Frígols" 
          fetchpriority="high"
          loading="eager"
          decoding="async"
          width="320"
          height="320"
          class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
          style="object-position: center 30%;" 
        />
      </div>
    </div>

    <!-- Greeting -->
    <p class="text-xl text-gray-600 dark:text-gray-400 mb-4 animate-fade-in" style="animation-delay: 0.1s;" data-i18n="hero.greeting">
      {hero.greeting}
    </p>

    <!-- Name -->
    <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 dark:text-white mb-6 animate-fade-in" style="animation-delay: 0.2s;">
      {hero.name}
    </h1>

    <!-- Title with Typewriter Effect -->
    <h2 id="typewriter-title" class="text-2xl md:text-3xl lg:text-4xl text-indigo-600 dark:text-indigo-400 font-semibold mb-20 min-h-[3rem]">
      <span class="typewriter-text"></span><span class="typewriter-cursor">|</span>
    </h2>

    <!-- Scroll Indicator -->
    <a href="#about" class="scroll-indicator block mt-65 animate-bounce cursor-pointer group" aria-label={currentLang === 'es' ? 'Ir a Sobre Mí' : 'Go to About Me'}>
      <span class="material-icons text-4xl text-gray-700 dark:text-gray-400 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-all duration-300 group-hover:scale-125 group-hover:drop-shadow-lg">
        keyboard_arrow_down
      </span>
    </a>
  </div>
</section>

<script>
  // Efecto typewriter
  function initTypewriter() {
    const typewriterText = document.querySelector('.typewriter-text');
    
    if (!typewriterText) return;

    // Limpiar cualquier contenido previo
    typewriterText.textContent = '';

    // Leer de window.__i18n__ (fuente de verdad SSR)
    const urlParams = new URLSearchParams(window.location.search);
    const currentLang = urlParams.get('lang') || 'es';
    const text = window.__i18n__?.[currentLang]?.hero?.title
      || window.__i18n__?.['es']?.hero?.title
      || '';
    let index = 0;
    
    function typeWriter() {
      if (index < text.length && typewriterText) {
        typewriterText.textContent += text.charAt(index);
        index++;
        setTimeout(typeWriter, 50);
      }
    }
    
    // Iniciar inmediatamente para evitar delay
    setTimeout(typeWriter, 300);
  }
  
  // Ejecutar cuando el DOM esté listo
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initTypewriter);
  } else {
    initTypewriter();
  }
</script>

<style>
  /* Cursor typewriter */
  .typewriter-cursor {
    animation: blink 1s step-end infinite;
    color: rgb(79, 70, 229); /* indigo-600 */
    text-shadow: 0 0 15px rgba(79, 70, 229, 0.7), 0 0 25px rgba(79, 70, 229, 0.4);
    font-weight: 600;
  }
  
  .dark .typewriter-cursor {
    color: rgb(129, 140, 248); /* indigo-400 */
    text-shadow: 0 0 18px rgba(129, 140, 248, 0.8), 0 0 30px rgba(129, 140, 248, 0.5);
  }
  
  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }
</style>

