---
interface Props {
  currentLang: string;
  translations: any;
  showNav?: boolean;
  showLogo?: boolean;
  showBackButton?: boolean;
}

const { currentLang, translations, showNav = true, showLogo = false, showBackButton = false } = Astro.props;
const { nav } = translations;
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/70 dark:bg-gray-900/70 backdrop-blur-sm border-b border-gray-200 dark:border-gray-700 transition-colors duration-300">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between relative">
    <!-- Back Button o Hamburger Menu (izquierda) -->
    <div class="flex items-center">
      {showBackButton ? (
        <a 
          href={`/?lang=${currentLang}`}
          class="flex items-center gap-2 px-3 py-2 bg-gray-900 text-white rounded-full hover:bg-gray-800 transition-all hover:scale-105 shadow-md font-medium text-sm"
          data-i18n-content="Volver al Inicio"
        >
          <span class="material-icons text-sm">arrow_back</span>
          <span class="hidden sm:inline">{currentLang === 'es' ? 'Volver al Inicio' : 'Back to Home'}</span>
        </a>
      ) : showNav && (
        <button
          id="mobile-menu-toggle"
          class="lg:hidden flex items-center gap-2 p-2 text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
          aria-label="Toggle menu"
        >
          <span class="material-icons text-2xl">menu</span>
          <span id="menu-sections-text" class="font-medium text-sm">{currentLang === 'es' ? 'Secciones' : 'Sections'}</span>
        </button>
      )}
    </div>

    <!-- Navigation Links (desktop - centrado absoluto) -->
    {showNav && (
      <div class="hidden lg:flex items-center gap-4 xl:gap-6 absolute left-1/2 transform -translate-x-1/2">
        <a href="#contact" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.contact" data-i18n-content={nav.contact}>
          {nav.contact}
        </a>
        <a href="#about" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.about" data-i18n-content={nav.about}>
          {nav.about}
        </a>
        <a href="#education" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.education" data-i18n-content={nav.education || 'Educación'}>
          {nav.education || 'Educación'}
        </a>
        <a href="#projects" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.projects" data-i18n-content={nav.projects || 'Proyectos'}>
          {nav.projects || 'Proyectos'}
        </a>
        <a href="#experience" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.experience" data-i18n-content={nav.experience || 'Experiencia'}>
          {nav.experience || 'Experiencia'}
        </a>
        <a href="#skills" class="text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium text-sm xl:text-base" data-i18n="nav.skills" data-i18n-content={nav.skills}>
          {nav.skills}
        </a>
      </div>
    )}

    <!-- Theme Toggle & Language Selector (derecha) -->
    <div class="flex items-center gap-2 sm:gap-3">
      <!-- Theme Toggle Button -->
      <button
        id="theme-toggle"
        class="p-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:border-blue-500 dark:hover:border-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all flex items-center justify-center"
        aria-label="Cambiar tema"
      >
        <span class="material-icons text-xl">dark_mode</span>
      </button>
      
      <!-- Language Selector -->
      <div id="lang-dropdown" class="relative">
        <button
          id="lang-dropdown-btn"
          class="flex items-center gap-1.5 px-2 sm:px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 font-medium cursor-pointer hover:border-blue-500 dark:hover:border-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all text-sm"
          aria-haspopup="listbox"
          aria-expanded="false"
        >
          <span id="lang-flag" class={`fi fi-${currentLang === 'en' ? 'gb' : 'es'} rounded-sm`}></span>{/* #6: mapping duplicado resuelto en script con langToFlag() */}
          <span id="lang-label">{currentLang.toUpperCase()}</span>
          <span class="material-icons text-base leading-none">expand_more</span>
        </button>
        <ul
          id="lang-menu"
          role="listbox"
          class="hidden absolute right-0 mt-1 w-28 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 shadow-lg overflow-hidden z-50"
        >
          <li
            role="option"
            data-lang="es"
            aria-selected={currentLang === 'es' ? 'true' : 'false'}
            class="flex items-center gap-2 px-3 py-2 cursor-pointer hover:bg-blue-50 dark:hover:bg-blue-900/30 text-gray-700 dark:text-gray-300 text-sm font-medium transition-colors"
          >
            <span class="fi fi-es rounded-sm"></span> ES
          </li>
          <li
            role="option"
            data-lang="en"
            aria-selected={currentLang === 'en' ? 'true' : 'false'}
            class="flex items-center gap-2 px-3 py-2 cursor-pointer hover:bg-blue-50 dark:hover:bg-blue-900/30 text-gray-700 dark:text-gray-300 text-sm font-medium transition-colors"
          >
            <span class="fi fi-gb rounded-sm"></span> EN
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Mobile Menu -->
  {showNav && (
    <div id="mobile-menu" class="hidden lg:hidden bg-white dark:bg-gray-900 border-t border-gray-200 dark:border-gray-700">
      <div class="px-4 py-4 space-y-3">
        <a href="#contact" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.contact" data-i18n-content={nav.contact}>
          {nav.contact}
        </a>
        <a href="#about" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.about" data-i18n-content={nav.about}>
          {nav.about}
        </a>
        <a href="#education" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.education" data-i18n-content={nav.education || 'Educación'}>
          {nav.education || 'Educación'}
        </a>
        <a href="#projects" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.projects" data-i18n-content={nav.projects || 'Proyectos'}>
          {nav.projects || 'Proyectos'}
        </a>
        <a href="#experience" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.experience" data-i18n-content={nav.experience || 'Experiencia'}>
          {nav.experience || 'Experiencia'}
        </a>
        <a href="#skills" class="block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors font-medium py-2" data-i18n="nav.skills" data-i18n-content={nav.skills}>
          {nav.skills}
        </a>
      </div>
    </div>
  )}
</header>

<script>
  // Establecer el valor del selector basado en la URL
  const urlParams = new URLSearchParams(window.location.search);
  const currentLang = urlParams.get('lang') || 'es';

  // #6: fuente de verdad única para el mapeo idioma → código de bandera
  const langToFlag = (lang) => lang === 'en' ? 'gb' : 'es';

  // Theme toggle button event listener
  const themeToggle = document.getElementById('theme-toggle');
  if (themeToggle && typeof window.toggleTheme === 'function') {
    themeToggle.addEventListener('click', () => {
      window.toggleTheme();
    });
  }

  // Dropdown de idioma
  const dropdownBtn = document.getElementById('lang-dropdown-btn');
  const dropdownMenu = document.getElementById('lang-menu');
  const langFlag = document.getElementById('lang-flag');
  const langLabel = document.getElementById('lang-label');

  if (dropdownBtn && dropdownMenu) {
    // Sincronizar estado visual con idioma actual
    if (langFlag) {
      langFlag.className = `fi fi-${langToFlag(currentLang)} rounded-sm`;
    }
    if (langLabel) langLabel.textContent = currentLang.toUpperCase();
    dropdownMenu.querySelectorAll('li[data-lang]').forEach(item => {
      item.setAttribute('aria-selected', item.getAttribute('data-lang') === currentLang ? 'true' : 'false');
    });

    // #7: AbortController para poder eliminar el listener global al destruir
    const dropdownController = new AbortController();

    // Abrir/cerrar
    dropdownBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = !dropdownMenu.classList.contains('hidden');
      dropdownMenu.classList.toggle('hidden');
      dropdownBtn.setAttribute('aria-expanded', String(!isOpen));
    });

    // Seleccionar idioma
    dropdownMenu.querySelectorAll('li[data-lang]').forEach(item => {
      item.addEventListener('click', () => {
        const ALLOWED_LANGS = ['es', 'en'];
        const lang = item.getAttribute('data-lang') || 'es';
        const safeLang = ALLOWED_LANGS.includes(lang) ? lang : 'es';
        document.body.classList.add('language-changing');
        // #4: limpiar la clase si la navegación no se completa
        const navigationTimer = setTimeout(() => {
          window.location.href = window.location.pathname + '?lang=' + safeLang;
        }, 200);
        window.addEventListener('pagehide', () => clearTimeout(navigationTimer), { once: true });
        setTimeout(() => document.body.classList.remove('language-changing'), 1000);
      });
    });

    // Cerrar al hacer click fuera
    document.addEventListener('click', () => {
      dropdownMenu.classList.add('hidden');
      dropdownBtn.setAttribute('aria-expanded', 'false');
    }, { signal: dropdownController.signal });

    // Limpiar listener global al salir de la página
    window.addEventListener('pagehide', () => dropdownController.abort(), { once: true });
  }

  // Toggle mobile menu
  const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');
  
  if (mobileMenuToggle && mobileMenu) {
    mobileMenuToggle.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
      const icon = mobileMenuToggle.querySelector('.material-icons');
      if (icon) {
        icon.textContent = mobileMenu.classList.contains('hidden') ? 'menu' : 'close';
      }
    });

    // Close menu when clicking on a link
    const mobileLinks = mobileMenu.querySelectorAll('a');
    mobileLinks.forEach(link => {
      link.addEventListener('click', () => {
        mobileMenu.classList.add('hidden');
        const icon = mobileMenuToggle.querySelector('.material-icons');
        if (icon) {
          icon.textContent = 'menu';
        }
      });
    });
  }
</script>

<style>
  header {
    animation: slideDown 0.4s ease-out;
  }

  @keyframes slideDown {
    from {
      transform: translateY(-100%);
    }
    to {
      transform: translateY(0);
    }
  }
</style>
